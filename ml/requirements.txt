# Core data + ML
pandas==2.2.2
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.1
lightgbm==4.5.0

# Supabase + HTTP
supabase==2.30.1          # 2.10+ required for sb_secret_... key format
websockets==15.0.1        # required by supabase realtime client
httpx==0.27.0
python-dotenv==1.0.1

# Market data (macro features: Nifty, VIX, USD/INR, US 10Y)
yfinance==0.2.40

# Parquet for fast feature snapshots (optional but useful)
pyarrow==17.0.0

# Hyperparameter tuning (optional — only needed for --tune)
optuna==3.6.1

# SHAP explanations (optional — only needed for train.py)
shap==0.45.1

# News sentiment
anthropic>=0.40.0       # Claude API SDK (0.40+ required for Haiku 4.5)
feedparser==6.0.11      # Google News RSS parsing

# CLI niceties
tqdm==4.66.4

# Zerodha Kite API (India VIX fetch — used by fetch_vix_kite.py + macro_features.py)
kiteconnect>=5.0.1
